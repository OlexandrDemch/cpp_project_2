#include <iostream>
using namespace std;

int main()
{
    int year;
    cout << "Vvedit rik: ";
    cin >> year;

    bool isLeapYear = (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);

    if (isLeapYear) {
        cout << "V " << year << " rotsi = 366 dniv" << endl;
    }
    else {
        cout << "V " << year << " rotsi = 365 dniv" << endl;
    }

    return 0;
}
